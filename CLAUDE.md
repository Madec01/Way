# WAY — contexte de reprise

Roguelite d'action 2D vue de dessus, navigateur, **un seul `index.html`** (Canvas 2D, JS vanilla, Web Audio, localStorage) + `assets/`. Français partout. Dépôt : `Madec01/Way`, branche `main`, publié sur GitHub Pages (madec01.github.io/Way). Tu es chef de projet + développeur principal ; l'auteur (Martin) donne carte blanche, veut un point court à chaque étape terminée, et **des propositions avant de coder quand il le demande**.

## Sources et assemblage
- Sources dans `dev/` (un module par fichier, ordre dans `dev/build.js`), assemblées par `node dev/build.js` → `index.html`. **Toujours éditer `dev/*.js` puis rebuild**, jamais `index.html` directement.
- Modules : `00_core` (Engine, Input, Camera, Fullscreen), `05_balance` (constantes d'équilibrage), `AudioEngine`, `content.js` (biome 1 + tout le contenu) et `content2.js` (biome 2), `10_content_api`, `15_sprites` (sprites, corps du joueur, musique), `20_progression`, `30_entities` (joueur, armes, projectiles, pickups, combat), `32_enemies` (ennemis + boss), `34_traps`, `36_modular`, `38_challenges`, `39_tempo` (Beat : horloge musicale calée sur `assets/music/tempo.json` ; Tempo : salle du tempo), `40_room` (état G, Room, Run), `50_ui`, `55_touch`, `57_attract`, `60_meta`, `70_debug` (panneau F1 + bot + `window.__autoplay`), `90_main`.
- Schéma des données : `SCHEMA.md`. Contenu chiffré : `CONTENT.md`. Rapports de test/équilibrage : `TEST-REPORT.md` (§10 = dernier rééquilibrage). Plan/état : `PLAN.md`.

## Tester
- `cd /home/user/way && python3 -m http.server 8766` puis Playwright/Chromium (`executablePath: '/opt/pw-browsers/chromium'`, ne jamais lancer `playwright install`) ; scripts de test dans le scratchpad de session (à recréer si absents) : balayage `__autoplay` par arme, test clavier/souris, test tactile, captures.
- `await __autoplay({ seed, timeScale: 40, render: false, mode: 'normal'|'test', weapon, skill, biome, maxRooms: 9, difficulty })` renvoie les stats (salle atteinte, niveau, kills, dégâts, cause de mort, niveau par salle).
- Avant chaque push : `node dev/build.js`, un balayage sans erreur JS, test clavier (8 OK) et tactile.

## État (septembre 2026)
Fait : 4 paliers (ADMISSION, LA SERRE, LA CONCESSION, LE SÉRAIL) × 9 salles, 8 armes, 8 compétences, 74 greffes, 16 passifs méta, boss + revanche avec mimétisme, salles modulaires, défis (salle 2 = salle aléatoire à défi garanti ; 6 et 7 à 60 % : capture, effondrement, séquence, lumières coupées, chrono ; plus de salle de pièges seule), objets au sol (bourse, arme d'essai, allié, reliques), apparence évolutive (nu → vêtements → armure → chevalier), menu en deux temps (écran-titre puis menu principal, effets calés sur la musique) et hub refondus, scène d'attraction en fond, tactile + zoom caméra + plein écran, musiques par biome (`assets/music/menu|hub|biomeN|bossN.mp3`, repli génératif), rééquilibrage passe 2 (jeu difficile : bot normal ne bat plus le mini-boss).

En attente de décision de l'auteur : **l'histoire**. L'ancien lore (`LORE.md`, « SUJET NEUF », Bureau d'Homologation) est **rejeté**. Le jeu s'appelle **WAY**. Proposition en cours (à valider, puis à intégrer dans les textes du jeu) : la Voie, route de montagne à Relais-machines construits par l'ingénieure Honorine Way ; le joueur est un Passeur portant le Pli ; ennemis = Détraqués ; Gardien du Relais = mini-boss qui revient avec la Fiche de voyageur ; jetons tamponnés au Poste 4 ; hub = Relais Zéro tenu par la Muletière ; Malle-poste qui ramène quand on tombe ; Relais 1 l'Accueil, Relais 2 les Jardins, Relais 3 les Cuisines ; Passeurs Marius et Léonie. Les textes en jeu portent encore l'ancien vocabulaire (Salle Zéro, greffes, crédits, Étalon…) tant que ce n'est pas validé.

## Règles posées par l'auteur
- **À chaque nouveau biome**, livrer sans qu'il ait à le demander, en plus du contenu : (1) les **prompts Suno** pour les musiques du palier — deux pistes d'exploration (`biomeN-1`, `biomeN-2`) et une piste de boss (`bossN`), style + paroles instrumentales, dans la direction déjà retenue (orchestral hybride, couleur propre au biome) ; (2) les **prompts PixelLab** pour les accessoires du biome — un prompt global de style, puis un prompt court par objet, avec le nom de fichier exact attendu par `PROP_DEFS` en tête de prompt (voir `assets/sprites/pixel/README.md`). Les deux dans le même message que l'annonce du biome, pas après relance.

## Conventions
- Contenu = données déclaratives avec `id` stables ; effets à hooks listés dans `SCHEMA.md` §2 ; pas de logique de gameplay dans `content*.js`.
- Pas de son « oscillateur nu » ; pas de dépendance externe, pas de build step pour jouer.
- Commits en français, un point court à l'auteur par étape, push sur `main`.

## Rythme et musique
- `Beat` (dev/39_tempo.js) avance avec l'horloge de simulation (`Time.now`) et se recale sur la position de l'`<audio>` en temps réel (Time.scale 1, non headless) : recalage franc au-delà de 120 ms, correction douce sinon. Autoplay accéléré et vitesse debug restent donc cohérents.
- `assets/music/tempo.json` est généré par `python3 dev/analyze_music.py` (librosa) : BPM, décalage du premier temps, tonalité par fichier. À relancer à chaque changement de piste. Piste absente du fichier → métronome interne 120 BPM (`Beat.info.internal`).
- **L'horloge d'un piège est choisie par piège, plus par salle** : `Room.trapTime(r, t)` renvoie `Beat.t` dès que le piège déclare `params.beats`, sinon `r.time`. Avant, c'était décidé par salle (`r.tempo.syncTraps`) : un piège rythmique posé hors de la salle 7 tournait à la bonne vitesse mais sur une phase sans rapport avec la musique. N'importe quelle salle peut désormais jouer en mesure.
- **Trajets continus calés sur le temps** : `beats.turn` = temps pour un tour (`laser_rotate`), `beats.trip` = temps pour un aller (`saw_rail`). `syncBeat` convertissait la période mais **jamais `angularSpeed` ni la vitesse du rail** : le bras croisait le joueur à un instant arbitraire et dérivait à l'infini. `speedMul` est volontairement écarté de ces deux conversions — un piège plus rapide en difficulté 3 sortirait du tempo.
- Poussée du joueur : `pl.kvx / pl.kvy`, amortissement de 10/s comme chez les ennemis. **La distance parcourue vaut l'impulsion divisée par 10** : 1440 px/s = 3 tuiles. C'est la monnaie des pièges « qui gênent au lieu de blesser ».
- Partition au sol : `Tempo.renderScore` (appelé pour toute salle depuis `Room.render`) marque les tuiles qui vont être frappées — coins dorés un temps avant, cadre plein un demi-temps avant. Deux formes que rien d'autre ne dessine : le damier du dancefloor remplit déjà des tuiles entières, un remplissage de plus s'y noierait. Recalculé au changement de demi-temps seulement (`Tempo.scoreAt`, 8 fois/s au plus), jamais par image — ce serait 312 tuiles × N pièges × 60.
- Salle du tempo = type `COMBAT_TEMPO` (salle 7 des deux biomes). Pièges : `params.beats = { period, active, telegraph, on }` (ou `{ every, telegraph, on }` pour bouches et tourelles) en temps musicaux, recalés à chaque appel par `Trap.syncBeat()` ; l'horloge des pièges y est `Beat.t` (`Room.trapTime`). Ennemis : `beatLock`/`beatDiv` posés par `Room.spawnEnemy`, `Enemy.armed()` retarde lunge/tir/charge/explosion au prochain temps. Joueur : `Tempo.playerAction` (tir et compétence) → `pl.beatMul` dans `Weapons.dmgOf`.
- Ne jamais poser une tourelle sur la tuile de la porte (x 23, y 6) : le bot refuse d'approcher une zone de danger permanente et reste bloqué sous la porte.
- Menu et hub en rythme : `UI.beatPulse()` (appelé par `renderAttractVeil` / `renderBackdrop`) pose les variables CSS `--beat` et `--down` sur `:root` (quantifiées au 1/40 pour limiter les recalculs de style) ; les règles sont en fin de `dev/style.css`. `Beat.update()` tourne une fois par pas dans `update()` de `90_main.js`, quel que soit l'état.
- Scripts de test : `menubeat.js` (menu/hub : variables CSS, piste, captures), `tempo.js` (BIOME=…, vérifie compte à rebours, spawns sur le premier temps, attaques en phase), `tempo_dmg.js` (LVL=…, dégâts par source), `dmgrooms.js` (run accélérée, dégâts et PV par salle).
- Anti-crépitement (AudioEngine) : limiteur en sortie (ratio 20, seuil −4 dB) après le compresseur ; fin de voix en fondu de 20 ms (`v.kill`, jamais de coupure sèche sauf `kill(true)`) ; espacement minimal par son (`GAPS`, 25 ms par défaut) et atténuation de rafale (−12 % par répétition rapprochée) ; saturateurs sans suréchantillonnage. Ramassage : `Pickups.streak` fait monter la hauteur d'un demi-ton par orbe (`opts.pitch`). Test : `sfxburst.js`.
- Routage audio : effets → compresseur → master ; musique → master directement (jamais par le compresseur des effets, sinon chaque transitoire d'effet fait chuter la musique en 3 ms = clic) ; master → limiteur doux (seuil −1,5 dB, relâchement 250 ms) → sortie. `AudioEngine.spectrum(n)` : analyseur sur le bus musique (visuels de la salle du tempo). `Camera.pulse` : impulsion de zoom remise à 0 par `Room.load`.
- Boss en rythme : `Boss.rhythmStep` (32_enemies.js) remplace le séquenceur à recharges quand `this.rhythm` (défaut, sauf `G.debug.noRhythm`). `Boss.patKind(p)` classe les patterns (big / util / small). `rb.crossed` est mis à jour chaque image dans `ai_boss` (même pendant un pattern) : ne pas recalculer le franchissement de temps ailleurs, sinon un temps « en retard » déclenche hors rythme. Salles de boss : `Tempo.createBoss` (tempo sans compte à rebours, `syncTraps: false`, `bigFlash` pour vignette et caméra). Test : `boss.js` (BIOME=… ROOM=5|9 LVL=…) journalise départs et actions avec la phase du temps.
- Musique par palier : `biome1-1.mp3` salles 1-4, `biome1-2.mp3` salles 6-8 (`Music.keyFor` bascule sur `G.room.index >= 6` ; replis biome1-2 → biome1-1 qui reprend sa position → biome1 ancien nom). Préchargé au premier geste. Toutes les pistes actuelles sont à 129,2 BPM (menu et biome 2-1 en ré mineur, biome 2-2 et boss en ré majeur).
- La musique respire : `Music.setState({hp01, slow, overdrive})` chaque pas depuis `Run.update` (filtre `musicFilter` via `AudioEngine.setMusicTone`, cœur `setHeartbeat`, vitesse `setMusicRate`) ; `Music.calm()` à `Room.clear`, `Music.uncalm()` à `Room.load`, `Music.dying(s, then)` à la mort, `Music.tapeStop()` (rampe JS de playbackRate, `st8.ramp` bloque `setState` pendant la rampe). `Music.play` bascule sur la mesure (`Beat.timeToNextBar`, max 2,2 s) ; entrée de boss = `cutMusic` + `bossBreath` + départ à 0. `Beat` colle à la piste sans lissage quand `Music.rate !== 1`.
- Sons accordés : `TUNE` (AudioEngine) = fréquence de référence par son → `nearestScale` sur la pentatonique de `key` (posée par `Beat` via `AudioEngine.setKey`) ; `opts.step` = degré (cascade de ramassage). `LEVELS` : effets répétitifs ≈ −6 dB. Test : `music_fx.js`, `death.js`.
- Hub : la colonne centrale est « Choisis ton niveau » (cartes `.card.level` avec numéro, difficulté ★, état Sélectionné / Cliquer pour choisir / Verrouillé, accroche `biome.tagline`, paires bonus/malus) + bouton « JOUER — Niveau n · NOM ». Préparation : deux étapes distinctes `.prepstep.weapon` (cyan, 1) et `.prepstep.skill` (violet, 2), chips bonus/malus, résumé « arme + compétence », bouton d'entrée qui nomme les deux choix. Ids inchangés (`#hub-enter`, `[data-biome]`, `[data-w]`, `[data-s]`, `#prep-go`, `#prep-room`, `#prep-level`).
- Hub : trois zones colorées avec `.colhead` (1 Personnage vert, 2 Mission orange, Boutique cyan) ; onglets boutique libellés Améliorations / Armes / Personnages / Fragments (clés internes inchangées : passifs, armes, sujets, fragments).
- Sortie audio : `master → WaveShaper tanh(1,15) → destination`. Ne jamais remettre un DynamicsCompressor derrière le compresseur des effets : dans Chrome, deux compresseurs en série font perdre ≈ 12 dB (mesuré avec `offline.js`, rendu OfflineAudioContext via `dev/audio-test.html`).
- Biome 3 (`dev/content3.js`, LA CONCESSION, western) : ennemis `enemy_coyote…enemy_crotale`, pièges `trap_moulin…trap_barbeles` (kinds existants, couleurs western), boss `boss_marshal` (faiblesse during_reload). Sprites : `SPRITE_DEFS` clés `*3` (créatures du tileset + `tint` propre au sprite, ou `prop` = icône rastérisée avec `roll`). Décor : `Sprites.loadProps()` au boot (fetch des SVG de `assets/sprites/western/`, `PROP_DEFS` couleur + taille de raster), `drawProp`, obstacles `kind` → `BLOCK_KIND`, `room.deco` → `drawDeco`. En `file://`, fetch échoue : les obstacles retombent sur le bloc métallique, les ennemis-accessoires sur leur forme de secours.
- À venir (PLAN.md §8) : refonte complète de l'interface mobile, jugée illisible et peu pratique sur téléphone. Gros chantier à traiter comme un jalon dédié, pas en retouches.
- Accessoires : `Sprites.pixelize()` transforme chaque icône SVG en sprite pixel art au chargement — rastérisation ×4 puis seuil d'alpha (bords nets : le lissage du navigateur, agrandi ensuite, donnait une frange floue), contour sombre d'un pixel, rampe de 3 tons avec lumière en haut à gauche. Les formes d'un pixel de large gardent le ton de base, sinon les éclairer mange l'objet. Les icônes trop détaillées pour leur taille (gemmes, scorpion, cobra) ressortent brouillonnes : relever `px` dans `PROP_DEFS` aide, mais le vrai remède est de les remplacer par un vrai sprite.
- Exports PixelLab : `node dev/install-sprites.js <dossier des .zip>` range tout seul. La cible est lue dans `metadata.json` → `objects[0].prompt`, dont le premier mot est le nom du fichier (l'auteur met `cactus.png<TAB>…` en tête de prompt) ; le nom du zip ment (PixelLab enlève les tirets). Chaque nouvel essai prend le premier emplacement libre, rien n'est écrasé. Les fichiers déposés dans la conversation arrivent sur le disque **seulement s'ils sont zippés** : une image collée est vue mais n'existe pas en fichier.
- Remplacer un accessoire par un vrai sprite : déposer le PNG dans `assets/sprites/pixel/<nom>.png` puis lancer `node dev/index-pixel.js` (il reconstruit `index.json` et signale les noms inconnus). Le PNG est repris tel quel, sans traitement, et l'icône correspondante n'est même pas chargée. Le nom doit être une clé de `PROP_DEFS`.
- Toutes les versions installées d'un accessoire **servent de variété en jeu** : `Sprites.variantOf(nom, k)` en tire une par obstacle à partir de sa position (`hash2(o.x, o.y)`, déterministe — la même salle est toujours meublée pareil). Deux cactus côte à côte ne sont donc pas le même dessin. `hash2` mélange vraiment : un `x * 31 + y * 17` naïf donnait la même parité à toute une rangée alignée, donc la même variante partout.
- Comparateur : mode Test → F1 → **Accessoires** (`Debug.toggleProps`), panneau en bas pour que la salle reste visible. Un clic **impose** une version le temps de la regarder (`Sprites.setVariant`, retenu dans `localStorage` clé `way.props`) ; « Variété » (`Sprites.clearVariants`) rend la main au tirage. « Copier la sélection » sort la liste des fichiers — sert à élaguer : ce qu'on garde dans le dépôt est ce qui tourne en jeu.
- `Sprites.drawProp` accepte `foot: true` (y est la ligne de sol, pas le centre) : un sprite carré dans une case plus haute flottait au-dessus de son socle. `BLOCK_GROW` agrandit certains accessoires au-delà de leur case (cactus 1,7 — un saguaro dépasse sa tuile), l'emprise de collision, elle, ne bouge pas.
- `floorCache` est vidé par `setVariant` : sans ça le sol déjà rendu garderait l'ancien accessoire.
- Terrain de salle (CONTENT.md §26) : `dev/35_terrain.js`, inséré entre `34_traps` et `36_modular`. Une salle déclare `terrain: [13 chaînes de 24 caractères]` (et/ou `terrainRects`), compilé par `Terrain.compile` en `room.grid` (`Uint8Array` de 312 codes) + des rectangles fusionnés poussés dans `room.obstacles` avec `terrain: true`. Types actuels : sol, mur, muret (`n`), moucharabieh (`:`), pont (`=`), eau (`~`), boue (`,`).
- **`pointBlocked(x, y, r, mask)`** : `mask` vaut `move` (défaut), `shot` ou `sight`. Les obstacles portent `stopsShot` et `blocksSight` (absents = vrai, comportement d'avant). Un muret a `stopsShot: false`, donc les balles le traversent — il faut donc **toujours passer le masque** dans du code nouveau, sinon un muret redevient un mur plein. `lineOfSight` consulte en plus `Terrain.screenAt` (le moucharabieh n'est pas un obstacle).
- Vitesse de surface : `Terrain.speedAt(x, y)` multiplie la vitesse du joueur (`Player.update`) et le `this.slow` des ennemis (`Enemy.update`). Un seul champ couvre marche, ruée, charge et dash. **Les boss en sont exclus** (`!this.isBoss`) : les ralentir casse leurs phrases rythmiques.
- Le dash du joueur franchit les obstacles `dashOver` (murets) : test dans `resolveRoomCollision`, limité à `e === G.player && e.dashing` — ne pas l'étendre aux ennemis, la charge qui s'arrête sur le muret est ce qui les rend gérables.
- Rendu : le terrain **statique** est peint une fois dans le cache du sol (`Terrain.paint`, appelé par `Sprites.drawFloor`) ; seul le miroitement de l'eau est redessiné par image (`Terrain.render`, appelé par `Room.render`). Ne jamais vider `floorCache` par image. La clé du cache inclut maintenant l'id de la salle : `floorSeed` vaut `(index * 7919) ^ 0x5bd1`, donc **il est identique pour toutes les salles d'un même index**, et sans l'id deux salles partageaient leur sol.
- Vérifications : `node dev/check-terrain.js` (hors ligne, casse le build si une porte est inatteignable) et `spawncheck.js` dans le scratchpad (Playwright : compile les 36 salles et signale tout spawn fixe, piège, entrée ou porte sur une tuile bloquée — il a déjà trouvé un spawn dans un obstacle en salle 3 de LA SERRE).
- Décor des salles : `Room.dress(room)` (40_room.js, appelé par `Room.load`) donne un `kind` du biome aux obstacles nus et sème 5 à 8 décors au sol ; table `Room.DRESS` par biome. `Sprites.PROP_DEFS` porte le dossier (`d`), la couleur et la taille de rastérisation de chaque accessoire — trois dossiers : `western/`, `lab/`, `greenhouse/`. Ne jamais remettre de rectangle plein derrière un accessoire (lu comme une « ombre carrée ») : ombre elliptique + socle bas seulement.
- Hub : le bouton `#hub-enter` est `position: sticky; bottom: 0` dans la colonne centrale, et les cartes de niveau non sélectionnées masquent leurs paires bonus/malus — sinon le bouton sort de l'écran dès qu'il y a trois niveaux.
- Boss : `Boss.patKind` classe les patterns (big / util / small) et le séquenceur rythmique choisit dans chaque classe — un pattern classé `util` n'est joué qu'en fin de première moitié de phrase, avec son propre index `rb.utilIdx`. Nouveaux kinds dans `runPattern` : `quake` (onde annulaire), `duel` (balle rapide + rechargement à découvert), `mines` et `roots` (zones posées dans `room.hazards` avec `owner: 'enemy'`, gérées dans `Room.update`). Ornements : `bossDef.crest` (`plate` / `crown` / `hat`) dessiné par `Boss.drawCrest`.
- **Les familles de pièges s'arment aussi sans vague.** `Tempo.onWave` n'est appelé qu'à partir de la 2ᵉ vague (la salle démarre sans piège, c'est voulu) : avec 4 familles pour 3 vagues, **les deux dernières ne s'armaient jamais, dans les quatre biomes**. `tryAnnounce` a maintenant un filet : passé 10 mesures sans nouveauté, la famille suivante s'annonce toute seule. Ne pas « corriger » ça en retirant le garde-fou `wavesStarted`, ça casserait le démarrage sans piège.
- Salle du tempo : `tp.groups` = familles de pièges (par `trap.id`, dans l'ordre de la salle) ; `Tempo.armGroup` en arme une de plus à chaque vague via `Tempo.onWave` (appelé depuis `Room.update` au déclenchement d'une vague). Ne pas remettre tous les pièges au « GO ».
- Défi « lumières coupées » : masque d'obscurité composé sur un calque hors écran (`darkC`) percé en `destination-out` par chaque source, puis teinte des faisceaux en `lighter` ; `Challenge.lit(c, x, y)` dit si un point est éclairé (bonus d'XP, objectif du bot). Phase `lure` (halo d'accueil) → `show` ; `Challenge.waveGate` bloque les vagues tant qu'on n'a pas rejoint le halo.
- Salle du tempo : `tp.pendingGroup` + `tp.cueBeats` = avertisseur de quatre temps avant l'arrivée d'une famille de pièges (`AudioEngine.tempoCue`, accordé). `groupIdx` vaut −1 au départ : aucun piège tant que la première vague n'est pas passée.
- Mix : les sons de pièges sont volontairement très en retrait (`LEVELS` : trapGas 0.45 → trapSpike 1.4) et ceux qui ont une hauteur sont accordés (`TUNE`) pour ne pas frotter contre la musique.
- Piège d'édition : ne jamais coller un commentaire `//` à la fin d'une ligne remplacée par script — il avale la suite de la ligne (deux régressions déjà). Remplacer la ligne entière, ou mettre le commentaire au-dessus.
- Lumières coupées : `LIGHT_PATTERNS` (38_challenges.js) = motifs, un seul actif ; `nextPattern(c)` bascule sur un temps fort après `patBars` mesures (jamais le même deux fois, jamais deux motifs durs à la suite). Kinds de sources : `round`, `rect` (axis v/h, `sweep` pour la traversée), `mirror`, `strobe`. `c.fade` = fondu d'entrée, `c.flash` = éclair du stroboscope.
- Biome 4 (`dev/content4.js`, LE SÉRAIL, oriental) : ennemis `enemy_derviche…enemy_djinn`, pièges `trap_sabres…trap_moucharabieh`, boss `boss_vizir`. Accessoires dans `assets/sprites/orient/` (28 SVG game-icons extraits de `@iconify-json/game-icons`). Sprites : clés `*4` de `SPRITE_DEFS`, avec `scale` (nouveau champ : agrandissement propre au sprite, utilisé par `boss4` pour donner au Vizir une silhouette haute et fine).
- Boss : deux kinds de patterns propres au Vizir dans `runPattern` — `sandstorm` (mur de sable traversant, brèche tirée au sort et annoncée dès la télégraphie) et `mirage` (doubles qui tirent la même salve puis s'effacent). Leur rendu est dans `Boss.renderPattern`, appelé depuis `Enemy.render`. Nouvelle règle de faiblesse `after_mirage` (posée dans le case `mirage`) : le vrai est démasqué juste après la salve. Ornement `crest: 'lamp'` avec `crestOver: true` (dessiné APRÈS le sprite, sinon il passe dessous) et `crestDy` (décalage vertical, indispensable quand le sprite est plus haut que le rayon du boss).
- Menu en deux temps (CONTENT.md §23) : `UI.showTitle()` (écran-titre) au démarrage, `UI.showMenu()` (menu principal centré) partout ailleurs — retour du hub, des crédits, bascule plein écran. Le DOM est `.menuscreen.splash` / `.menuscreen.main` (l'ancienne classe `.menu2` n'existe plus) ; le bandeau `.stamp` et le pied `.menufoot` sont en position absolue pour que le bloc titre puisse être vraiment centré. `menuFx` porte l'état (phase, ondes, flash, graves, volume, inactivité, boutons révélés, action en attente), `menuUpdate` avance tout depuis `UI.update`, `renderMenuFx` dessine le calque canvas (appelé par `render()` de `90_main.js`, après les deux branches). Les scripts de test doivent d'abord franchir l'écran-titre (`await page.click('.menuscreen.splash')` puis ~2,7 s d'attente) avant de cliquer `#btn-normal` / `#btn-test`.
- Arme d'essai : `Room.load` ne peut pas la poser en salle 1, le joueur n'ayant pas encore d'arme (l'écran de préparation vient après `Run.start`) — une run sur quatre plantait (`weaponDropRoom` tiré dans [1, 3, 6, 7]). Le tirage est mis en attente (`G.room.pendingWeaponDrop`) et posé par `Room.begin()`, après `Run.equip`.
- Menu : `barSync(fn)` cale une validation sur la mesure suivante — à ne jamais utiliser pour le plein écran ni une entrée en plein écran tactile, qui exigent le geste de l'utilisateur (le geste part tout de suite, seule la transition attend).
- Vitesse des lumières : toute destination est bornée par `vMax = G.player.stats.speed` (`reach = vMax × k × moveDur`, k = 0,85 normal / 1,05 lucioles / 1,55 pointe). Avant, la destination était tirée n'importe où dans la salle : le halo allait en moyenne 1,2× et en pointe 20× plus vite que le joueur, donc impossible à suivre (mesuré avec `lightspeed.js` / `lightmoves.js`). Le balayage fait un va-et-vient au lieu de se téléporter au bord opposé. Un cercle en pointillés marque la destination pendant le trajet (`renderOverlay`) : sans lui on court derrière la lumière au lieu de couper au plus court. Ne jamais remettre un `pt()` libre comme destination.
- Mouvement des lumières : pas de `lerp` par image (saccadé quand la cible change souvent). Chaque source a `fx/fy → tx/ty` parcouru en `moveDur` (1 ou 2 temps, parfois 0,5 = déplacement éclair) avec une interpolation `easeInOutCubic` sur `moveK` ; une nouvelle destination n'est tirée que si la précédente est atteinte ET sur un temps.
- Atelier rythme (CONTENT.md §28) : `dev/80_atelier.js`, entre `70_debug` et `90_main`, ouvert par **F2**. Il crée une run jetable sur une salle vierge (`room_atelier`, type `TRAP`, porte fermée) et **refabrique les pièges sans recharger la salle** (`Atelier.apply`) — recharger couperait la musique, et c'est elle le repère de travail.
- **Partition libre** : `params.beats.hits` = liste des temps frappés dans une boucle de `bars` mesures. `Trap.cycleHits` remplace `Trap.cycle` quand elle est là, `Trap.fireCycle` fait la même chose pour les bouches de feu et les tourelles (qui tirent sur `p.every`, pas sur `cycle`). L'ancien format `{ period, on }` est intact : ne pas le retirer, une quarantaine de déclarations s'en servent.
- `Music.seekBeat(n)` place la lecture sur le n-ième temps de la piste (via `AudioEngine.seekMusic`) : c'est ce qui rend la règle de l'atelier cliquable et la boucle possible. Sans fichier de piste (musique générative), le déplacement échoue et l'atelier se contente d'avancer sa fenêtre — ne pas supposer que le déplacement réussit.
- En mode pose, `Atelier.posing()` coupe le tir du joueur (une ligne dans `Player.update`) : sans ça chaque pose déclenche un coup d'épée.
- `Input` ignore le clavier quand la frappe part d'un `input`, `textarea` ou `select` — sinon taper une largeur dans l'atelier fait courir le joueur.
- Comparer des instants musicaux dans un test : viser le **milieu** des subdivisions. Sur les bornes exactes, `t - loop * P` perd assez de précision (10⁻¹⁴) pour changer l'état d'un piège ; `cycleHits` a un epsilon de 10⁻⁹ pour qu'un coup posé pile au début de la boucle ne tombe pas dans l'arrondi.
- Pièges tireurs : `Trap.shotState(rt, defaut)` remplace l'ancien découpage en cycles. Il renvoie `shotIdx` (dernier coup dû), `warnIdx` (prochain) et `warm` (chaleur d'annonce, 0 → 1 vers le coup). Deux règles à ne pas défaire : un coup est un **instant** (sinon des coups rapprochés s'avalent entre eux, l'annonce durant plus longtemps que l'écart), et le déclenchement compare au **dernier coup joué** (`lastShot`), jamais à un compteur croissant — dans l'atelier la lecture revient en arrière et un compteur bloquait le piège pour de bon.
- Nouveaux kinds (CONTENT.md §29) : `emitter` (salves en couronne / spirale / éventail / visé, avec rafales) et `laser_beam` (rayon fixe taillé au bord de la salle). Les rafales sont espacées en **temps réel** (`Time.now`), pas en temps musical, pour survivre au retour en arrière de la boucle.
- `params.color` surcharge la couleur d'un piège pour une instance (lu dans le constructeur via `this.p`). `beats.active: 0` (ou absent) veut dire « toute la boucle » côté atelier — indispensable aux pièges à trajet continu, qui sinon tournent dans le noir et se rallument n'importe où.
- Mode test de l'atelier : `room.noScore` coupe `Tempo.renderScore`, et `Atelier.render` ne dessine ni grille ni cadres. C'est la salle telle qu'elle sera jouée.
- Décor animé (CONTENT.md §30) : `dev/33_anim.js`, chargé avant `34_traps`. `AnimProp` n'a ni collision ni dégât — ne jamais lui en donner, c'est ce qui permet d'en couvrir une salle sans toucher à la difficulté. `beatPulse(beats, rt)` est l'enveloppe commune (partagée avec les pièges par la forme `beats.hits`). Rendu en deux passes : `Anim.render` après le décor (sous les entités), `Anim.renderOver` dans `Room.renderFx` (lumières additives).
- `Sprites.addCustom(nom, dataURL)` enregistre une image de l'auteur comme accessoire, gardée dans `localStorage` (`way.props.custom`) et rechargée par `Sprites.loadCustoms()` au démarrage. Elle ne remplace pas le dépôt dans `assets/sprites/pixel/` : c'est un aperçu immédiat, pas une livraison.
- `Sprites.clearFloor()` vide le cache du sol : obligatoire après avoir repeint du terrain (le terrain statique est peint dans ce cache).
- Atelier : `Atelier.apply()` refabrique tout sans recharger la salle, mais `Room.load` (donc `startRoom`) réhabille la salle — d'où l'appel à `apply()` juste après, qui lui retire son décor au sol. La porte est reforcée fermée à chaque image dans `Atelier.update` : une salle vidée l'ouvrirait et le joueur sortirait de l'atelier.
- Compagnons (CONTENT.md §31 et §34) : `dev/31_pets.js`, chargé entre `30_entities` et `32_enemies`. L'état vit dans **`G.pets`, une liste** ; `G.pet` reste comme raccourci en lecture sur `G.pets[0]`. Tout ce qui balaie les compagnons (rendu, dégâts, tri en profondeur, modes) doit parcourir la liste — un attelage en compte deux. Ils survivent aux changements de salle (`Room.load` appelle `snap()`) et meurent avec la run (`Run.start` repart d'une liste vide). Un compagnon **ne meurt jamais** : seul celui qui attire les coups (`def.taunt`) a des PV, et à zéro il est sonné puis revient — ne pas transformer ça en perte définitive, le joueur n'a aucun moyen de le soigner.
- `Enemy.pickTarget` regarde les leurres **et** un compagnon qui attire les coups ; le contact lui fait des dégâts via `Pets.hurt`. C'est le seul endroit où un compagnon peut être blessé.
- Le compagnon est trié avec les ennemis par profondeur dans `render()` de `90_main.js` (il passe devant ou derrière selon son y), et son badge est dessiné par `Pets.renderHud` juste après `UI.renderHud`.
- Dalles peintes (CONTENT.md §32) : `params.cells` (liste de tuiles) remplace le rectangle pour `tile_color` et `tile_lift`. Toujours passer par `AnimProp.eachCell` pour parcourir un motif — le rectangle et la liste doivent rester interchangeables. Le damier se calcule sur `tx + ty`, pas sur l'indice dans la liste, sinon un motif peint de travers perd son damier.
- Atelier : le glissé (mousedown + mousemove) peint terrain et dalles ; `dragSeen` empêche de traiter deux fois la même tuile dans un geste, et le glissé n'efface jamais (seul le clic isolé bascule).
- Partitions longues (CONTENT.md §33) : dans l'atelier, `bars` est la longueur de la PARTITION (jusqu'à 128 mesures) et `win` la largeur de la FENÊTRE affichée. Ne pas les refondre en un seul réglage : c'est ce qui permet d'écrire un niveau de trois minutes tout en gardant une grille lisible. Les positions des coups sont absolues dans la partition, la règle est numérotée en absolu, et la lecture boucle sur la fenêtre.
- `hitAround(hits, t, P, loop)` (33_anim.js, utilisé aussi par 34_traps.js) trouve le dernier coup passé et le prochain par dichotomie. Ne pas revenir à un balayage : une partition de morceau entier porte des centaines de coups, lus par piège et par image. L'astuce qui autorise un seul candidat de chaque côté : la durée d'activité est la même pour tous les coups.
- `beats.__sec` / `__L` : cache de conversion en secondes posé sur l'objet `beats` lui-même (un cache à un seul emplacement serait piétiné par les éléments suivants dans la même image).
- Modèle de pinceau : `st.tpl[brush]` a la forme d'un élément posé. `tune()` l'édite quand rien n'est choisi, `fromBrush()` le clone à la pose (en retirant `cells` et `path`, qui n'ont de sens que sur un élément réel).
- Sons calés sur un temps : passer par `AudioEngine.<son>({ delay })`, où `delay` est l'instant visé en secondes à partir de maintenant (`Beat.timeToBeat(1).at`). Déclencher au franchissement du temps met le son 8 à 17 ms en retard, systématiquement — mesuré, et audible sur un métronome. Ne pas revenir à un déclenchement réactif pour un son qui doit tomber sur le temps.
- `Beat.lag` (calibré dans l'atelier, gardé dans `Meta.profile.lag`) retarde l'horloge de la latence de sortie pour que l'image colle à ce qu'on entend. Le métronome compense en avançant son clic d'autant. Les autres sons calés sur les temps partent d'autant plus tard : c'est le compromis assumé tant qu'ils sont déclenchés et non programmés.
- Compagnons, progression : `Meta.profile.pets` (débloqués) et `Meta.profile.pet` (choisi au hub, donné par `Run.start` sans bandeau). Le mode test les débloque tous. Le ramassage en run **remplace** le compagnon du hub : un seul à la fois, c'est ce qui en fait un choix. Les animaux de l'auteur sont livrés `unlocked: true` et à 0 crédit — ne pas laisser un profil neuf sans aucun compagnon, la mécanique ne se découvrirait jamais.
- `Room.load` replace le compagnon (`snap()`) **après** avoir positionné le joueur, jamais avant : sinon il reste à l'ancienne place et met une seconde à traverser la salle.
- Marquage (`mark`) : `e.markUntil` / `e.markMul`, lus dans `Combat.hitEnemy`. C'est le seul multiplicateur de dégâts qu'un compagnon applique — les autres tapent eux-mêmes.
- Comportement `sting` : la portée de piqûre se compare au rayon d'ORBITE, pas à un rayon de contact, sinon l'abeille tourne juste au-delà de sa propre portée et ne touche jamais.
- Atelier « Amis » (CONTENT.md §31) : quatrième établi, seul endroit d'où viennent les compagnons et les personnages-amis. `CONTENT.pets` ne contient **que les animaux de l'auteur** (`content5.js`) — ne pas y remettre d'animaux « pour l'exemple », l'auteur veut ceux de ses proches. Les gabarits sont sous `CONTENT.petsExemples`, jamais chargés.
- `Content.invalidate()` (10_content_api.js) : à appeler après TOUT ajout de contenu à chaud. L'index par id est mis en cache à la première lecture ; sans invalidation, `Content.character(nouvelId)` retombe sur le premier de la liste sans rien signaler — le bug était invisible à l'œil, le personnage jouait juste sous une autre identité.
- Visage d'un personnage : `char.face` = nom d'accessoire, collé à la place de la tête par `Sprites.drawBody`. Un personnage qui porte un visage est plafonné au palier 2 (corps dessiné en pixels) : au palier 3 le jeu passe à la planche de sprites et le visage disparaîtrait.
- Photos importées : recadrées au carré et réduites à 64 px avant d'être gardées. Ne pas stocker l'image d'origine — dix visages de téléphone rendraient `content5.js` et le `localStorage` inutilisables.
- `dev/content5.js` est un fichier **généré** : il est remplacé en entier par l'export de l'établi Amis. Ne rien y écrire à la main.
- Images de l'auteur : **ne jamais agrandir ni lisser une image qui tient déjà dans 128 px**. Le jeu est en pixel art (sources de 16 px rendues ×3) ; un 32×32 fourni par l'auteur est du travail fini, l'agrandir en lissant le détruit. Seules les photos d'appareil sont réduites, et là le lissage sert. Le dessin reste toujours en `imageSmoothingEnabled = false` — sauf le visage collé sur un corps, qui est une photo réduite à 24 px et gagne au lissage.
- Tailles de référence : `TILE = 48`, `SCALE = 3`, joueur 48 × 75 px à l'écran. Un compagnon naît à 64 px = ×2 d'une image de 32, donc pixels carrés. Ne pas revenir à 48 « pour caler sur la tuile » : ×1,5 sur une source de 32 px rééchantillonne et salit.
- `char.body` (sprite entier) l'emporte sur `char.face` (visage collé) dans `Sprites.drawBody`, et court-circuite la planche de sprites. Il est ancré sur la ligne de sol du corps standard (`y + 25 × échelle`) pour que les pieds tombent au même endroit.
- Vues directionnelles : un sprite d'ami est soit un nom, soit `{ s, e, n }`. **Toujours passer par `Sprites.pickDir(spec, dir)`** pour en tirer un nom — les deux formes doivent rester interchangeables, et une vue absente retombe sur le sud. `Sprites.dirFrom(dx, dy)` donne `{ dir, flip }` ; l'ouest est l'est retourné, ne pas prévoir de quatrième image.
- La direction vient du **déplacement réel**, pas de l'intention : le compagnon compare sa position à celle de l'image précédente (`px/py` → `dx/dy` lissés). Un compagnon poussé ou en orbite regarde ainsi où il va vraiment.
- `Sprites.gait(walk)` : rebond, balancement et respiration d'un sprite sans planche d'animation. À utiliser pour tout sprite unique en mouvement ; à l'arrêt il ne rend qu'un souffle lent. Ne pas le confondre avec une animation — le jour où l'auteur fournit des planches, c'est `gait` qui s'efface, pas l'inverse.
- Planches d'animation (CONTENT.md §31) : `Sprites.addSheet(nom, url, taillleCase)` stocke l'image **telle quelle** — ne jamais la faire passer par `readImg`, qui recadre et redimensionne : la grille de cases ne tomberait plus juste. `Sprites.CLIPS` porte les cadences et le drapeau `once` ; `Player.animStep` choisit le clip par priorité (mort > ramasse > tir > marche > repos).
- `sheet.foot` : fraction de case occupée jusqu'au bas du dessin, mesurée à l'import. Le sprite est posé pour que ce bas tombe sur la ligne de sol du corps standard — sans ce repère, un personnage flotte d'autant de pixels que la marge sous ses pieds, et le défaut est invisible tant qu'on ne le compare pas au corps standard.
- Taille d'affichage d'une planche : **un multiple entier de la case** (×2 par défaut). Une case de 48 px affichée en 88 « pour caler sur le joueur » rééchantillonne et salit le dessin.
- `dev/content5.js` peut aussi être écrit par un script (c'est ainsi que Martin est entré) : `FRIEND_SHEETS[nom] = { fw, url }` plus un `CONTENT.characters.push`. Le fichier reste néanmoins remplacé en entier par l'export de l'atelier.
- Modes de compagnon (`PET_MODES`, 31_pets.js) : `always`, `call`, `none`. Le mode vit dans `Meta.profile.petMode` et est posé par `Pets.give`. En `call`, le compagnon commence `away` et ne se met à jour ni ne se dessine tant qu'il n'est pas appelé (touche `pet`, KeyC) — vérifier `away` (ou `hidden()`) partout où l'on touche à un compagnon depuis l'extérieur (les dégâts encaissés le font déjà). `Pets.setMode` pose le mode sur **tout** l'attelage.
- Bonus d'équipe : `CONTENT.pairs` + `Content.pairOf(charId, petId)`, appliqué par `Pets.applyPair` (multiplicateur sur l'animal, buff `pair` sur le joueur). Les buffs `pair` et `solo` sont posés avec `roomOnly: false` — avec `true` ils sauteraient au changement de salle, et le défaut ne se voit qu'en salle 2.
- Un compagnon animé n'a pas d'accessoire à son nom : passer par `Pets.icon` (badge) et `Sprites.sheetCanvas` (cartes du hub) plutôt que `drawProp`, sinon il n'apparaît que comme une pastille de couleur.
- Clips de compagnon : `idle`, `walk`, `attack`, `hurt`. Un animal qui n'a que repos et marche retombe dessus sans rien casser — c'est le cas courant, ne pas exiger la panoplie complète.
- Attelage inséparable : `def.duo` nomme le second animal, `def.duoName` le nom du couple, et le second porte `hidden: true` pour ne pas s'acheter seul. `Pets.give` crée les deux, `Pets.title` affiche `duoName`. Un attelage occupe **une seule place** : ce n'est pas une façon d'avoir deux compagnons, c'est un compagnon qui vient à deux.
- Une équipe (`CONTENT.pairs`) se déclare sur le **meneur** de l'attelage (`pet: 'pet_choupi'`), jamais sur le second : `Content.pairOf` regarde `G.pets[0]`. Le bonus est ensuite appliqué aux deux par `Pets.applyPair`.
- Un personnage sans planche d'animation prend un `sprite` existant en attendant les dessins de l'auteur. Il est jouable, il n'a simplement pas encore son visage — ne pas le retirer du contenu pour autant, son équipe en dépend. Plus aucun personnage n'est dans ce cas (Martin, Gabriel et Jean ont leurs planches) ; il reste les trois chats, qui n'ont qu'une pastille de couleur.
- Entrée clavier du joueur (`30_entities.js`, branche « ni bot ni tactile ») : `firing` **et** `wantSkill` sont deux affectations distinctes. Ne jamais laisser la seconde glisser en fin de commentaire de la première — la compétence ne partirait plus ni à Espace ni au clic droit, et rien d'autre ne le signalerait.
- Pas de tenue progressive (CONTENT.md §35) : le personnage est habillé dès la salle 1 et son apparence ne bouge plus de la run. `Sprites.bodyTier`, l'option `tier` et `G.debug.forceTier` ont été supprimés — ne pas les réintroduire « pour marquer la progression », l'auteur a tranché : quatre tenues par personnage, c'est trop à dessiner.
- Un personnage qui porte un `face` est le seul à garder le corps dessiné : il reçoit le corps de la planche coupé sous le cou (`cut = 10`), sinon la planche entière recouvrirait le visage. Le corps en pixels ne sert plus que de secours tant que la planche n'est pas chargée.
- Nombre d'images d'une planche : `framesOf` (15_sprites.js) coupe les cases vides **de fin**, jamais celles du milieu. PixelLab livre 7 images dans une grille 3×3 ; sans ça le personnage s'efface deux temps sur neuf. Ne pas remplacer par « la première case vide arrête la planche » : une case vide au milieu est une image voulue.
- L'arme en main se place avec `Sprites.handY(sprite, y, opts)` (45 % du corps au-dessus des pieds), jamais à un décalage fixe depuis `player.y` : chaque type de corps a sa ligne de sol et sa hauteur, et un nombre en dur remet l'arme à l'entrejambe dès qu'un personnage plus grand arrive.
- Ligne de sol d'un compagnon à planche : `y + 25`, la même que le joueur (`Pet.render`, variable `sol`). L'ancienne formule `y + taille × 0,34` faisait flotter les petits plus haut que les grands. Un compagnon à image fixe (`drawProp`) garde l'ancienne convention, centrée sur `y`.
- Taille d'affichage d'un animal : ×2 de sa case, quelle que soit la case (48 → 96, 32 → 64). Ne pas réduire un animal « pour qu'il soit plus petit que le chien » : à ×1 son grain est deux fois plus fin que tout le reste, et l'auteur a tranché pour la taille naturelle.
- Un test d'image qui mesure une boîte englobante doit exclure l'ombre (alpha 0,32 → 82 ; seuil 200), sinon il valide l'ombre à la place des pattes — c'est arrivé, et la doc a affirmé une ligne de sol fausse. Dire dans le libellé ce que l'on mesure.
- `drawSheet` avec `foot: true` pose le bas du DESSIN sur `y` : `oy = −(foot − 0,5) × dh`, sans `− dh/2`. Seul `Pet.render` l'utilise ; le joueur refait le même calcul inline dans `drawBody`.
- **Tests** : `dev/test/` (lire son README). `cd dev/test && npm install` une fois, puis `node run.js` (tout, ~15 min) ou `node run.js duo uno`. Un test = `require('./lib').test(async ({ page, ok, entrer, salle, run }) => …)`. Ne plus écrire de test dans un dossier de session : s'il ne vit pas dans `dev/test/`, l'auteur ne peut pas le relancer et il n'existe pas.
- **Format** : Prettier (`.prettierrc`, printWidth 140) sur `dev/*.js` et `dev/test/*.js`, jamais sur `content*.js` (des tables : une entrée par ligne) ni `content5.js` (généré). `cd dev/test && npm run format` avant de commiter. Les 173 lignes encore au-delà de 140 sont des chaînes ou des regex insécables.
- **Mode strict** : chaque fichier de `dev/` commence par `'use strict';` (sauf `content5.js`, généré). Une variable assignée sans déclaration lève maintenant une erreur au lieu de créer un global — c'est voulu.
- **Noms de premier niveau** : `build.js` refuse deux fichiers qui déclarent le même `const`/`let`/`function`/`class`. Si le build échoue avec « est déclaré au premier niveau dans … », renommer, ne pas retirer le contrôle.
- Mort du joueur : `Player.render` dessine le corps si `char.anim.death` existe, et `Run.onPlayerDeath` diffère l'écran de fin de `clipLen(death) + 0,35 s` (jamais pour le bot). Ne pas remettre `if (this.dead) return` en tête du rendu : les planches de mort redeviendraient invisibles.
- Clip « ramasse » : `PICK_CLIP_KINDS` (30_entities.js) — jamais `coin`, `xp`, `fragment`, qui tombent par dizaines.
- Sauvegarde (SCHEMA.md §8) : `SAVE_VERSION`, `MIGRATIONS[v]` (une fonction par saut, ne jamais en modifier une existante), fusion profonde, copie de secours `way_save_secours_v<N>`. Tout changement de forme du profil = version +1 et une migration, même vide. Ne jamais faire `Object.assign(fresh(), d)` : c'est ce qui perdait les sous-champs.
- Erreurs : `Rapport` (00_core.js) note tout dans `way_journal` et fournit « Copier le rapport ». Ne pas ajouter de `catch` vide sans y appeler `Rapport.noter` si l'erreur concerne le joueur.
- Export de l'atelier : les images importées (`IMG_SLOTS`) ne sortent qu'avec `avecPhotos`, après `confirm` ; le `pseudo` d'un copain remplace son `name` dans `charDef`. Ne pas court-circuiter la demande d'accord « pour aller plus vite ».
- F2 ouvre l'atelier en mode test seulement (`G.mode === 'test'`). `?atelier=1` dans l'URL reste possible.
